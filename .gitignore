# Everything generated lives under dist/ (pages, build/ intermediates,
# cache/ bundle cache, logs/, release tarballs).
dist/

.DS_Store
*.egg-info/
.enju/
.mypy_cache/
*.pyc
__pycache__/
*.pyo
.pytest_cache/
*.swp
.venv/

# generated corpus seeds (regenerable via `python -m atlas.build_corpus`)
data/corpus/seeds/

# internal-only docs (research, roadmap, upstream-liaison notes) — on disk, not published
docs/internal/
# KG-Registry submission drafts — local staging; submitted to the external registry by hand
docs/kg-registry/
