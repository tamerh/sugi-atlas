"""Page-assembly primitives — the bits between the section bundles and the
final Hugo-rendered markdown.

  declarative.py   - deterministic one-sentence lead (the first thing AI agents
                     extract as the gene's definition)
  jsonld.py        - (next) schema.org Gene/BioChemEntity emitter
  provenance.json  - (next) per-fact upstream-source mapping
"""
